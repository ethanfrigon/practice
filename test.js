let newObj = {
    eyes: 'blue',
    'number of eyes': 2,
    nose: 'beak like',
}

Object.keys(newObj).forEach(key => console.log(`${key} and ${newObj[key]}`));

let output = (newObj.eyes);
console.log(output);
